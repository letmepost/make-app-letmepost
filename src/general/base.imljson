{
	"baseUrl": "https://api.letmepost.dev",
	"headers": {
		"Authorization": "Bearer {{connection.apiKey}}"
	},
	"response": {
		"error": {
			"message": "[{{statusCode}}] {{body.error.code}}: {{body.error.message}}"
		}
	},
	"log": {
		"sanitize": ["request.headers.authorization"]
	}
}
